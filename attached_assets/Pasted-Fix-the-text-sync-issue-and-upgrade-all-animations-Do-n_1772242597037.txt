Fix the text sync issue and upgrade all animations. Do not change 
any colors, bloom values, fog, or scene geometry from the previous fix.

1. FIX TEXT SYNC - title and background must change at exactly the same time:
   - In the click handler, inside the GSAP onComplete callback (at the 
     end of the zoom tween), call loadArcScene() AND set new title text/color 
     AND trigger the GSAP title fade-in all at the same instant
   - Remove any setTimeout or secondary delay between scene load and text update
   - Swap text content first, then animate: 
     titleEl.textContent = arc.title THEN gsap.fromTo(titleEl, ...)

2. ZOOM TRANSITION - cinematic flying feel:
   - During zoom tween also tween camera.rotation.x from 0 to 0.12 
     then back to 0 (using a yoyo or two sequential tweens)
   - At the 0.7s midpoint of the zoom, flash a full-screen white div 
     (position fixed, z-index 999, background white) from opacity 0 → 0.5 → 0 
     over 0.4s using GSAP
   - Apply CSS filter blur(4px) to the canvas at zoom start, 
     remove it (blur(0px)) at zoom complete using GSAP

3. TITLE ENTRANCE ANIMATION - elastic cinematic feel:
   - Title: gsap.fromTo opacity 0→1, scale 1.4→1.0, y 30→0, 
     duration 0.9s, ease "back.out(1.7)"
   - Tagline: animate letter-spacing from 0.4em → 0.1em + opacity 0→1, 
     duration 1.1s, ease "power2.out", start 0.2s after title
   - Add a thin gold horizontal scan line div (height 2px, color #FFCD00, 
     position absolute, width 100%) that sweeps from x:-100% to x:100% 
     across the title area on each arc entrance using GSAP, then fades out

4. IDLE CAMERA - living cinematic movement between clicks:
   - In animate() loop add slow orbit: 
     camera.position.x = Math.sin(t * 0.08) * 3
   - Gentle breathing on Y: 
     baseY + Math.sin(t * 0.15) * 0.8 (where baseY = 5)
   - Mouse parallax adds ON TOP of this base movement (don't replace it)

5. OBJECT ANIMATIONS - upgrade in render loop:
   - All primary scene meshes: add scale pulse 
     mesh.scale.setScalar(1 + Math.sin(t * 1.2) * 0.025)
   - Particles: add per-particle X wobble using 
     positions[i*3] += Math.sin(t + i * 0.1) * 0.01

6. PROGRESS DOTS - animate on change:
   - When dot becomes active, use GSAP to scale it 1 → 1.6 → 1 
     with ease "elastic.out(1, 0.4)"
   - Add a CSS ripple ring: a pseudo-element circle that expands from 
     the active dot and fades out using @keyframes ripple animation

7. VIGNETTE - dynamic mouse-following:
   - On mousemove, update the radial-gradient center position to 
     slightly follow the cursor (move 10% toward cursor position) 
     creating a subtle living vignette feel
